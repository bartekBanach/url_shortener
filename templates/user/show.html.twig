{% include 'default/_crud_show.html.twig' with {
    page_title: 'title.user_details',
    item: user,
    fields: ['id', 'email', 'isVerified'],
    labels: {
        'id': 'label.id',
        'email': 'label.email',
        'isVerified': 'label.is_verified'
    },
    dateFields: ['createdAt'],
    back_to_list_path: path('user_index')
} %}