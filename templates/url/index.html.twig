{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.url_list'|trans }}
{% endblock %}

{% block body %}
    <h1 class="text-2xl font-bold mb-4">{{ 'title.url_list'|trans }}</h1>
    {% if urls is defined and urls|length %}
        <div class="overflow-x-auto">
            <table class="min-w-full bg-white border border-gray-200">
                <thead>
                <tr class="w-full bg-gray-100 border-b">
                    <th class="py-2 px-4 text-left">{{ 'label.id'|trans }}</th>
                    <th class="py-2 px-4 text-left">{{ 'label.long_url'|trans }}</th>
                    <th class="py-2 px-4 text-left">{{ 'label.short_url'|trans }}</th>
                    <th class="py-2 px-4 text-left">{{ 'label.created_at'|trans }}</th>
                    <th class="py-2 px-4 text-left">{{ 'label.actions'|trans }}</th>
                </tr>
                </thead>
                <tbody>
                {% for url in urls %}
                    <tr class="border-b">
                        <td class="py-2 px-4">{{ url.id }}</td>
                        <td class="py-2 px-4 truncate">{{ url.longUrl }}</td>
                        <td class="py-2 px-4">{{ url.shortUrl }}</td>
                        <td class="py-2 px-4">{{ url.createdAt|date('Y/m/d') }}</td>
                        <td class="py-2 px-4">
                            <a href="{{ path('url_show', {id: url.id}) }}" class="text-blue-500 hover:underline" title="{{ 'action.view'|trans }}">
                                {{ 'action.view'|trans }}
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    {% else %}
        <p class="text-gray-600 mt-4">
            {{ 'message.empty_list'|trans }}
        </p>
    {% endif %}
{% endblock %}
